#! /bin/bash

. ./container/secrets/secrets.env

script_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
. ${script_dir}/functions.sh

update_repos $1 # in re-brand branch, force repo(s) update before building

cloud_build $1
